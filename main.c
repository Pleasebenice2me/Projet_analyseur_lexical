#include "analyse_lex.h"
#include "Jeton.h"
#include <stdio.h>

int main(int argc, char *argv[]){
    int tab;
    char str();
    // Ask the user to type a number
    printf("Type your function: \n");
    // Get and save the number the user types
    scanf("%c", &str);
    tab = analyselexical(char str);
    return 0;
}